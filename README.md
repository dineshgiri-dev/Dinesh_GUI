# Dinesh_GUI

Qt/QML sources for this snapshot live in **`Smart_Atonomous_Robot_2026-04-04/`**. Configure and build from that directory (open `CMakeLists.txt` there in Qt Creator, or run CMake with that folder as the source path).
